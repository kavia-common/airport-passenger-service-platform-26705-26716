# airport_facility_service environment variables
# Copy to your runtime environment (.env is managed by the orchestrator/infra; do not commit secrets).

# PostgreSQL connection (env-driven; used by spring.datasource.url)
DB_HOST=localhost
DB_PORT=5001
DB_NAME=airport_platform
DB_USER=appuser
DB_PASSWORD=change-me

# Optional: Spring profile
SPRING_PROFILES_ACTIVE=default
